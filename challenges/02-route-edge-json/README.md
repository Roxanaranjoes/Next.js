# Reto 02 — Route Handler Edge JSON

Crea un handler Edge que responda `{ "ok": true }` en GET.

Criterios de aceptación
- Exporta `runtime = 'edge'`.
- Exporta `GET()` que retorna `Response` con `content-type: application/json`.
