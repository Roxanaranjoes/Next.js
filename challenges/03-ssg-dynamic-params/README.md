# Reto 03 — SSG dinámico con `generateStaticParams`

Implementa `generateStaticParams()` que devuelva al menos 3 slugs.

Criterios de aceptación
- Export por nombre.
- Devuelve un array con `{ slug: string }`.
